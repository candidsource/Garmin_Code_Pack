
. ${TGT_BASE_FS}/EBSapps.env run

##LAKSecho "disable jobs as system user" | tee -a ${APPLOG}
#LAKSecho ${SYSTEM_PWD} | sqlplus "/nolog" @${SCRIPT}/${ENV}_disable_scheduler_jobs_02.sql | tee -a ${APPLOG}

echo "Update User Profile for ICM Forms Launch" | tee -a ${APPLOG}
##LAKSecho ${t_APPS_PWD} | sqlplus "/nolog" @${SCRIPT}/${ENV}_update_users_profile.sql | tee -a ${APPLOG}

echo "Unbbreak dba job 7743" | tee -a ${APPLOG}
##LAKSecho ${t_APPS_PWD} | sqlplus "/nolog" @${SCRIPT}/${ENV}_unbroken.sql 7743 | tee -a ${APPLOG}

echo "Update Concurrent table with node Name" | tee -a ${APPLOG}
##LAKS${SCRIPT}/../bin/${ENV}_update_conc_mgr_nodes.sh | tee -a ${APPLOG}

echo "Create cloning related database packages" | tee -a ${APPLOG}
##LAKSecho ${t_APPS_PWD} | sqlplus "/nolog" @${SCRIPT}/${ENV}_XX_COMPILE_POST_CLONE_PACKAGE.sql | tee -a ${APPLOG}

echo "Update IBY tables" | tee -a ${APPLOG}
##LAKSecho ${t_APPS_PWD} | sqlplus "/nolog" @${SCRIPT}/${ENV}_insert_iby_bep_acct_opt_vals.sql | tee -a ${APPLOG}

echo "Scrub customer email addresses into the database" | tee -a ${APPLOG}
echo ${t_APPS_PWD} | sqlplus "/nolog" @${SCRIPT}/${ENV}_XX_POST_CLONE_STEPS_1.sql | tee -a ${APPLOG}

echo "Scrub Credit Card Data" | tee -a ${APPLOG}
##LAKSecho ${t_APPS_PWD} | sqlplus "/nolog" @${SCRIPT}/${ENV}_XX_POST_CLONE_STEPS_2.sql | tee -a ${APPLOG}

echo "XX_POST_CLONE_STEPS_3" | tee -a ${APPLOG}
##LAKSecho ${SYSTEM_PWD} | sqlplus "/nolog" @${SCRIPT}/${ENV}_XX_POST_CLONE_STEPS_3.sql ${S_PDB_NAME} ${S_PDB_NAME} | tee -a ${APPLOG}

echo "Scrub customers email address to inactive" | tee -a ${APPLOG}
##LAKSecho ${t_APPS_PWD} | sqlplus "/nolog" @${SCRIPT}/${ENV}_scrub_email_address.sql ${S_PDB_NAME} ${S_PDB_NAME} | tee -a ${APPLOG}

echo "Post Clone script XX_POST_CLONE_STEPS_4" | tee -a ${APPLOG}
##LAKSecho ${t_APPS_PWD} | sqlplus "/nolog" @${SCRIPT}/${ENV}_XX_POST_CLONE_STEPS_4.sql | tee -a ${APPLOG}

echo "Post Clone script XX_POST_CLONE_STEPS_4C" | tee -a ${APPLOG}
##LAKSecho ${t_APPS_PWD} | sqlplus "/nolog" @${SCRIPT}/${ENV}_XX_POST_CLONE_STEPS_4C.sql | tee -a ${APPLOG}

echo "Post Clone script XX_POST_CLONE_STEPS_6" | tee -a ${APPLOG}
##LAKSecho ${t_APPS_PWD} | sqlplus "/nolog" @${SCRIPT}/${ENV}_XX_POST_CLONE_STEPS_6.sql | tee -a ${APPLOG}

echo "Post Clone script XX_POST_CLONE_STEPS_5" | tee -a ${APPLOG}
##LAKSecho ${t_APPS_PWD} | sqlplus "/nolog" @${SCRIPT}/${ENV}_XX_POST_CLONE_STEPS_5.sql | tee -a ${APPLOG}

echo "Post Clone Update user level profile values for iSupplier Users" | tee -a ${APPLOG}
##LAKSecho ${t_APPS_PWD} | sqlplus "/nolog" @${SCRIPT}/pos_upg_usr.sql | tee -a ${APPLOG}

echo "Restore Agile property files After the clone"
##LAKScp -p ${SCRIPT}/${ENV}_CommonLogin.properties $GARM_JAVA_TOP/CommonLogin.properties
##LAKScp -r ${SCRIPT}/${TGT_RUN_FS}_custom${PDB_NAME}_`hostname`.env $INST_TOP/appl/admin/custom${PDB_NAME}_`hostname`.env

echo "Wallet creation for Payment Module" | tee -a ${APPLOG}
##LAKS${SCRIPT}/../bin/${ENV}_wallet_payment_module.sh| tee -a ${APPLOG}
